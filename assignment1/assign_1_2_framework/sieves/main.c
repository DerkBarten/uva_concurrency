#include <stdio.h>
#include <stdlib.h>

#include <pthread.h>

int main() {

int N = 30;
int p = 2;
int sieve[N];

void generate_numbers(list* generator, int start_value) {

    while(true) {
         // make new list entry
          // make new list entry.next = list.start
          // make new list entry
    }
    // make new list entry
    // push a new value on the list
}